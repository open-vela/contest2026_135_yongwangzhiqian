# BK7258 SDK CLI-B verification

- Date: 2026-08-20
- Scope: host SDK rebuild-interface migration; no real SDK or firmware build
- Conclusion: PASS for retiring the importer/registry compatibility surface

## Verified outcome

- `bk7258.py sdk rebuild` now owns source build, temporary AP profile
  application, UART object recompilation/archive replacement, provenance and
  the locked bundle/manifest/provenance replacement transaction.
- The base SDK version is parsed from the team manifest. The canonical AP
  profile is `ap.config`; an explicit variant uses the
  `ap-<variant>.config` convention. Production Python repeats no current
  version, profile filename, profile macro or manifest SDK path literal.
- The old importer, SDK registry/framework receipt command, r2/r3/r4 profile
  names and all `tools/bk7258/tests/` implementation tests are removed. The
  test-directory retirement was an explicit owner decision.

## Direct host evidence

- `bk7258.py sdk verify --role all`: PASS for the real CP bundle (424 files,
  content `438c1bf1...c7be`) and AP bundle (707 files,
  `09a83cc5...a196`).
- `bk7258.py sdk verify --version v3.1.1.9-sdio4 --role ap`: PASS for 707
  files, content `1f9520b1...81cf`.
- `bk7258_framework.py validate` and `framework-check`: PASS.
- Build-plan generation/verification: PASS, identity
  `57c0c8d3...6faed`.
- Package prepare/verify: PASS, source-build identity
  `19750074...ad02`.
- Modified shell syntax, CLI parsing/executable mode, residual-reference
  scans, developer-absolute-path scans and `git diff --check`: PASS.
- Independent read-only source review found no P1 SDK-B defect.

## Deliberate verification boundary

- No maintained Python test suite remains under `tools/bk7258/`.
- Before its owner-directed deletion, the temporary SDK-B behavior fixture
  exercised successful CP/AP/SDIO4 rebuild staging, UART symbol rejection,
  incomplete-state rejection, explicit replace and injected three-output
  rollback. That fixture is not retained as a project contract.
- A real SDK source rebuild and real toolchain transaction were not run.
  Firmware build, signing, package delivery, Flash/J-Link and hardware were
  also not performed.
