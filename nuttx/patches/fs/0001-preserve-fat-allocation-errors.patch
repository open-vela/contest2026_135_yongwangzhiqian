--- a/fs/fat/fs_fat32.c
+++ b/fs/fat/fs_fat32.c
@@ -679,6 +679,11 @@
     {
       cluster = fat_extendchain(fs, cluster);
 
+      if (cluster <= 0)
+        {
+          return cluster < 0 ? cluster : -ENOSPC;
+        }
+
       if (cluster < 2 || cluster >= fs->fs_nclusters + 2)
         {
           return -EIO;
@@ -701,6 +706,11 @@
   if (i == new_num_clu - 1)
     {
       cluster = fat_extendchain(fs, cluster);
+
+      if (cluster <= 0)
+        {
+          return cluster < 0 ? cluster : -ENOSPC;
+        }
 
       if (cluster < 2 || cluster >= fs->fs_nclusters + 2)
         {
--- a/fs/fat/fs_fat32util.c
+++ b/fs/fat/fs_fat32util.c
@@ -1205,9 +1205,9 @@
         }
       else if (startsector < 2)
         {
-          /* Oops.. this cluster does not exist. */
-
-          return 0;
+          /* An invalid existing chain is not a full-volume condition. */
+
+          return -EIO;
         }
       else if (startsector < fs->fs_nclusters + 2)
         {
