# Baseline: NuttX 76354c637858ecb0aa4601629327acb6f44a26bb
#
# mfrc522_read() must not inspect an uninitialized UID when card detection
# succeeds but PICC selection fails. Preserve successful formatting and the
# existing NULL/zero-length buffer behavior while returning the select error.
#
--- a/drivers/contactless/mfrc522.c
+++ b/drivers/contactless/mfrc522.c
@@ -1472,6 +1472,7 @@
   FAR struct inode *inode;
   FAR struct mfrc522_dev_s *dev;
   FAR struct picc_uid_s uid;
+  int ret;
 
   inode = filep->f_inode;
 
@@ -1488,7 +1489,12 @@
 
   /* Now read the UID */
 
-  mfrc522_picc_select(dev, &uid, 0);
+  memset(&uid, 0, sizeof(uid));
+  ret = mfrc522_picc_select(dev, &uid, 0);
+  if (ret < 0)
+    {
+      return ret;
+    }
 
   if (uid.sak != PICC_TYPE_NOT_COMPLETE)
     {
