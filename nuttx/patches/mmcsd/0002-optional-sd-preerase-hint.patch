--- a/drivers/mmcsd/Kconfig
+++ b/drivers/mmcsd/Kconfig
@@ -77,6 +77,16 @@
 		multiple-block writes but require single-block reads.
 		Explicit multi-block read ioctls exceeding this limit are rejected.
 
+config MMCSD_SDIO_NO_PREERASE
+	bool "Omit optional SD pre-erase hint"
+	default n
+	depends on MMCSD_SDIO
+	---help---
+		Do not send ACMD23 (SET_WR_BLK_ERASE_COUNT) before SD multi-block
+		writes. This optional performance hint is separate from CMD23
+		(SET_BLOCK_COUNT). Select only for media/controller combinations
+		that require writes without the pre-erase hint.
+
 config MMCSD_MMCSUPPORT
 	bool "MMC cards support"
 	default y
--- a/drivers/mmcsd/mmcsd_sdio.c
+++ b/drivers/mmcsd/mmcsd_sdio.c
@@ -2096,6 +2096,7 @@
       return ret;
     }
 
+#ifndef CONFIG_MMCSD_SDIO_NO_PREERASE
   /* If this is an SD card, then send ACMD23 (SET_WR_BLK_ERASE_COUNT) just
    * before sending CMD25 (WRITE_MULTIPLE_BLOCK).  This sets the number of
    * write blocks to be pre-erased and might make the following multiple
@@ -2126,6 +2127,8 @@
           return ret;
         }
     }
+
+#endif
 
   /* Data to the RPMB is programmed with the WRITE_MULTIPLE_BLOCK(CMD25),
    * in prior to the command CMD25 the block count is set by CMD23,
